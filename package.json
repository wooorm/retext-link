{
  "name": "retext-link",
  "version": "0.1.0",
  "description": "Retext link support",
  "license": "MIT",
  "keywords": [
    "retext",
    "link",
    "url",
    "uri"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/wooorm/retext-link.git"
  },
  "author": "Titus Wormer <tituswormer@gmail.com>",
  "devDependencies": {
    "eslint": "^0.8.0",
    "istanbul": "~0.3.0",
    "jscs": "^1.6.0",
    "mocha": "^1.20.0",
    "retext": "^0.2.0-rc.2",
    "retext-ast": "^0.1.2",
    "retext-visit": "^0.1.1"
  },
  "scripts": {
    "test": "node_modules/.bin/_mocha --reporter spec --check-leaks -u exports spec/retext-link.spec.js",
    "test-travis": "node_modules/.bin/istanbul cover node_modules/.bin/_mocha --report lcovonly -- --reporter spec --check-leaks -u exports spec/retext-link.spec.js",
    "lint": "npm run lint-api && npm run lint-test && npm run lint-style",
    "lint-api": "node_modules/.bin/eslint index.js --env node --env browser --rule 'quotes: [2, single], consistent-return: false'",
    "lint-test": "node_modules/.bin/eslint spec/retext-link.spec.js --env node --env mocha --rule 'quotes: [2, single], no-loop-func: false'",
    "lint-style": "node_modules/.bin/jscs index.js spec/retext-link.spec.js --reporter=inline",
    "coverage": "node_modules/.bin/istanbul cover node_modules/.bin/_mocha -- -- spec/retext-link.spec.js",
    "make": "npm run lint && npm run coverage"
  }
}
